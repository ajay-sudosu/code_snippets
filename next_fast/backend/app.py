# from flask import Flask
# from flask_apscheduler import APScheduler

# app = Flask(__name__)

# def test_job():
#     print('I am working...')

# scheduler = APScheduler()
# scheduler.init_app(app)
# scheduler.start()
# scheduler.add_job(id='kdavinder310@gmail.com', func=test_job, trigger='interval', seconds=1)